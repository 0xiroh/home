/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['drive.google.com', 'lh3.googleusercontent.com', "encrypted-tbn0.gstatic.com"],
    }
}

module.exports = nextConfig
